// Chrome storage wrapper for BitFS extension state
//
// Uses chrome.storage.local for wallet data (encrypted)
// Uses chrome.storage.session for temporary session keys

// TODO: implement get<T>(key: string) -> Promise<T | null>
// TODO: implement set<T>(key: string, value: T) -> Promise<void>
// TODO: implement remove(key: string) -> Promise<void>

export {};
