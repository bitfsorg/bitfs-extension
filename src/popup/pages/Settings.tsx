export function Settings() {
  return (
    <div style={{ padding: 16 }}>
      <h2 style={{ fontSize: 20, fontWeight: 600 }}>Settings</h2>
      <p style={{ color: "#999" }}>Extension settings will appear here.</p>
      {/* TODO: network selection (mainnet / testnet) */}
      {/* TODO: daemon URL configuration */}
      {/* TODO: auto-lock timeout */}
    </div>
  );
}
