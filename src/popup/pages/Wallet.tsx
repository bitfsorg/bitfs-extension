export function Wallet() {
  return (
    <div style={{ padding: 16 }}>
      <h2 style={{ fontSize: 20, fontWeight: 600 }}>Wallet</h2>
      <p style={{ color: "#999" }}>
        No wallet configured. Create or import a wallet to get started.
      </p>
      {/* TODO: wallet creation / import UI */}
      {/* TODO: display address, balance, transaction history */}
    </div>
  );
}
